# Frontend

The demo UI is a single plain-HTML/JS page served by the gateway itself from
`backend/src/main/resources/static/index.html` — no build step, works on the
local cluster (http://localhost:8080) and on Render out of the box.
